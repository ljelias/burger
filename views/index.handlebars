<h1>Great Burgers!</h1>

<h2>Burgers that are available!</h2>

<ul>
  {{#each burgers}}
    {{#unless claimed}}
      {{> burgers/burger-block taken=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are taken!</h2>

<ul>
  {{#each burgers}}
    {{#if claimed}}
      {{> burgers/burger-block taken=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add burger names!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerup">Next burger up:</label>
    <input type="text" id="burgerup" name="name">
  </div>

  <button type="submit">Add next burger</button>
</form>
